# Use the server-provided last modification date
timestamping = on

# Wait 60s before timing out
timeout = 60

# Retry when download fails
tries = 3

# Even when connection was refused
retry_connrefused = on

# Add missing file extensions
adjust_extension = on

# Ignore robots.txt and meta robots
robots = off

# Print the HTTP server response
server_response = on
